// #include "b.hpp"
// #include "c.hpp"
#include <iostream>
#include <cstdlib>
#include <ctime>
int main(void)
{
    // A *t = new B();
    // A *b = new C();

    // t->test();
    // b->test();
    // return (0);
//     int low_dist  = 1;
//   int high_dist = 6;
//   for( int repetition = 0; repetition < 10; ++repetition )
    // std::cout << 1 + std::rand() % ( 2 ) << std::endl;

    // std::srand(std::time(NULL));
    // std::srand( ( unsigned int )std::time( nullptr ) );
    // std::cout << rand() % 2;

    std::string abc = "     *\n    ***\n   *****\n  *******\n *********\n    ***\n";
    std::cout << abc << std::endl;
}